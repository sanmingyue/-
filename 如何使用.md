# 📦 预设自动更新使用指南

## 🎯 快速开始

### 1️⃣ 更新预设只需要做这些：

1. **修改预设内容**
   - 打开：`src/自动更新预设/预设.json`
   - 粘贴你的新预设内容

2. **修改预设名称**（可选）
   - 打开：`src/自动更新预设/名称.txt`
   - 修改第一行的名称

3. **更新日志**（可选）
   - 打开：`src/自动更新预设/更新日志.md`
   - 添加本次更新的说明

---

## 🚀 发布更新

### 方式一：使用命令行（推荐）

在项目文件夹打开终端，依次执行：

```bash
pnpm run build          # 打包
git add .               # 添加修改
git commit -m "更新预设" # 提交（引号里可以写更新说明）
git push                # 推送到GitHub
```

### 方式二：一键发布脚本

直接在终端执行：

```bash
pnpm run publish
```

这个命令会自动完成打包、提交、推送全部步骤。

---

## 📝 在酒馆中使用

在你的脚本中添加这行代码：

```javascript
import 'https://cdn.jsdelivr.net/gh/sanmingyue/-/dist/自动更新预设/index.js'
```

**注意：** CDN更新可能有 5-10 分钟延迟，发布后稍等片刻再测试。

---

## 📂 文件说明

| 文件路径                       | 作用     | 是否必须修改 |
| ------------------------------ | -------- | ------------ |
| `src/自动更新预设/预设.json`   | 预设内容 | ✅ 每次更新   |
| `src/自动更新预设/名称.txt`    | 预设名称 | 🔶 需要改名时 |
| `src/自动更新预设/更新日志.md` | 更新记录 | 🔶 建议记录   |

---

## ⚙️ 其他设置

### CDN地址说明
- GitHub仓库：`https://github.com/sanmingyue/-`
- CDN地址：`https://cdn.jsdelivr.net/gh/sanmingyue/-/dist/自动更新预设/index.js`

### 如果需要清除CDN缓存
访问这个网址强制刷新：
```
https://purge.jsdelivr.net/gh/sanmingyue/-/dist/自动更新预设/index.js
```

---

## 💡 小贴士

- ✨ 修改完预设后记得先本地测试
- 📦 每次发布前记得先 `pnpm run build` 打包
- 🔄 更新后可能需要等待几分钟CDN才会生效
- 📝 建议在更新日志中记录每次修改的内容

---

**最后更新：** 2025-10-29

